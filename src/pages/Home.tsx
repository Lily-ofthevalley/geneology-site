import HeroSection from "../components/HeroSection";

const Home = () => {
  return (
    <div className="flex flex-col items-center w-full">
      <HeroSection />
    </div>
  );
};

export default Home;
